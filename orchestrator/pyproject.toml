[project]
name = "orchestrator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sourav", email = "sourav@brandshark.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
orchestrator = "orchestrator:main"

[build-system]
requires = ["uv_build>=0.12.18,<0.13.0"]
build-backend = "uv_build"
